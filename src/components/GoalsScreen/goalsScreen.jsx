import React from "react";

const GoalsScreen = () => {

    // Allow user to set monthly expenditure goals according to different categories


  return (
    <div>
          <h1>Set monthly expenditure goals</h1>
          <form>
              <input placeholder="Input goal name"/>
          </form>
    </div>
  );
};

export default GoalsScreen;
